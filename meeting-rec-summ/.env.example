# OpenAI API key used by summarization, Whisper transcription, and image description.
OPENAI_API_KEY=

# Optional. Set to Streamlit to enable the configured Streamlit login flow.
DEPLOYMENT_ENVIRONMENT=
